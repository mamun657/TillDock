<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="0dp"
    android:layout_marginVertical="6dp"
    app:cardBackgroundColor="@color/surface_default"
    app:cardCornerRadius="14dp"
    app:cardElevation="0dp"
    app:strokeColor="@color/border_default"
    app:strokeWidth="1dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="14dp"
        android:paddingVertical="12dp">

        <TextView
            android:id="@+id/cart_item_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textColor="@color/text_strong"
            android:textSize="15sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/cart_item_line_total" />

        <TextView
            android:id="@+id/cart_item_line_total"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/text_strong"
            android:textSize="15sp"
            android:textStyle="bold"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <TextView
            android:id="@+id/cart_item_sku"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="@color/text_muted"
            android:textSize="12sp"
            app:layout_constraintTop_toBottomOf="@id/cart_item_name"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/cart_item_unit_price" />

        <TextView
            android:id="@+id/cart_item_unit_price"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="@color/text_muted"
            android:textSize="12sp"
            app:layout_constraintTop_toBottomOf="@id/cart_item_line_total"
            app:layout_constraintEnd_toEndOf="parent" />

        <LinearLayout
            android:id="@+id/cart_item_qty_row"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            app:layout_constraintTop_toBottomOf="@id/cart_item_sku"
            app:layout_constraintStart_toStartOf="parent">

            <ImageButton
                android:id="@+id/cart_item_decrement"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:background="@drawable/bg_qty_button"
                android:contentDescription="@string/cart_decrement"
                android:src="@drawable/ic_qty_minus"
                android:tint="@color/text_strong" />

            <TextView
                android:id="@+id/cart_item_quantity"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:minWidth="32dp"
                android:gravity="center"
                android:textColor="@color/text_strong"
                android:textSize="15sp"
                android:textStyle="bold" />

            <ImageButton
                android:id="@+id/cart_item_increment"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:background="@drawable/bg_qty_button"
                android:contentDescription="@string/cart_increment"
                android:src="@drawable/ic_qty_plus"
                android:tint="@color/text_strong" />
        </LinearLayout>

        <TextView
            android:id="@+id/cart_item_stock"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:paddingHorizontal="8dp"
            android:paddingVertical="2dp"
            android:background="@drawable/bg_stock_pill"
            android:textColor="@color/text_muted"
            android:textSize="11sp"
            app:layout_constraintTop_toBottomOf="@id/cart_item_sku"
            app:layout_constraintEnd_toEndOf="parent" />

        <ImageButton
            android:id="@+id/cart_item_remove"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_marginTop="10dp"
            android:layout_marginStart="8dp"
            android:background="@drawable/bg_qty_button"
            android:contentDescription="@string/cart_remove"
            android:src="@drawable/ic_close"
            android:tint="@color/danger_default"
            app:layout_constraintTop_toBottomOf="@id/cart_item_sku"
            app:layout_constraintStart_toEndOf="@id/cart_item_qty_row" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>